# Session Handoff - 2026-05-21

## Context
Projet `delegate-mcp` : serveur MCP générique multi-fournisseurs pour Claude Code, permettant de déléguer des sous-tâches à des modèles moins chers (GLM, MiniMax, Kimi, DeepSeek…) via leurs API flat-fee. La session s'est terminée au démarrage — aucun travail de code n'a été effectué.

## Completed
- Installation et rechargement du plugin claudy dans la session courante
- Création du dossier `specs/handoffs/` et du présent fichier de handoff

## In Progress
- Aucun travail en cours — session démarrée puis terminée immédiatement

## Next Steps
1. Définir la prochaine fonctionnalité ou tâche à implémenter sur `delegate-mcp`
2. Vérifier que le serveur MCP fonctionne correctement (`npm run dev` ou `npm run build && node dist/server.js`)
3. Vérifier la configuration `~/.config/delegate-mcp/providers.yaml` et les clés API exportées

## Key Files
- `src/server.ts` - Point d'entrée du serveur MCP
- `src/router.ts` - Logique de routage des requêtes vers les fournisseurs
- `src/config.ts` - Chargement de la configuration YAML
- `src/providers/factory.ts` - Factory pour instancier les providers
- `src/providers/anthropic-compat.ts` - Adaptateur protocole Anthropic
- `src/providers/openai-compat.ts` - Adaptateur protocole OpenAI
- `src/tools/definitions.ts` - Définitions des outils MCP exposés (delegate_bulk, delegate_long_context, delegate_reasoning, delegate_to, list_providers)
- `providers.example.yaml` - Exemple de configuration des fournisseurs

## Blockers / Notes
- Pas de dépôt git initialisé (le projet n'est pas sous git)
- Version actuelle : 0.1.0
- Le projet est un outil interne Triptyk (MIT)
